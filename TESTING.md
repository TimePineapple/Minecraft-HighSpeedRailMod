# highSpeedRail 1.2.6 Dedicated Server 验收

自动构建不能替代游戏内物理测试。使用 Minecraft 1.21.11 Dedicated Fabric Server 和未安装本模组的客户端，分别以默认及 Experimental Minecart Improvements 控制器开服验收。

建议配置：

```json
{
  "enable": true,
  "maxSpeed": 4.0,
  "activeBlocks": 128,
  "accelerationSeconds": 30
}
```

先运行 `/highspeedrail get`，确认：

- `version=1.2.6`
- `effectiveActivationBlocks=136`
- `speedUnit=track-centerline-blocks-per-tick`
- `slopePhysics=no-uphill-or-downhill-gravity-adjustment`
- 启动控制器和缓存陆地/水中原版水平上限正确
- 平轨/坡轨原版沿轨目标、`aAccel` 与四项 `aBrake` 正确

## 激活与配置边界

1. 普通轨、弯曲普通轨和不足阈值的动力轨全程保持原版。
2. 普通轨进入第一格动力轨的当 tick 保持原版；下一 tick 脚下仍为已供电动力轨且前方满足阈值时才接管。
3. 使用 `activeBlocks=9` 测试前方 16、17、18 格完整动力轨：16 不激活，17 和 18 激活；当前脚下铁轨不计数。
4. 未加载边界不得降低门槛、加载新区块、误判真实终点或启动制动。
5. 静止矿车先由原版建立方向；模组不猜测静止方向。
6. 验证旧配置迁移、整数秒校验、非法 JSON 保持最后合法配置，以及命令写回。

## 沿轨速度与坡轨

1. `maxSpeed=4` 时，长平轨每 tick 水平推进 4 格。
2. 45° 长坡每 tick 水平和垂直各推进约 `2.828` 格，三维沿轨距离为 4。
3. 四向上坡和四向下坡连续运行：`HIGH_SPEED` 沿轨速度不因坡度变化，上坡不减速，下坡不加速。
4. 分别验证坡底、坡顶、连续坡、弯轨接坡以及一个 tick 跨越平轨、弯轨、坡轨的混合路径。
5. 从平轨进入坡轨、从坡轨回到平轨时不得跳速、倒退、反弹或短暂离轨。
6. 最终实体速度向量 Y 为 0；实际高度必须按坡面连续吸附。
7. 载人和空车均能超过原版上限，且同配置下沿轨位移一致。

## 加速、制动与交回

1. 默认 `maxSpeed=1.2`、`accelerationSeconds=5` 时，`aAccel=0.012`，从沿轨速度 0 经 100 tick 达到 1.2。
2. 任意起速重新激活时从实际沿轨速度接续；修改秒数只影响下一加速阶段。
3. 分别在平轨、坡轨以及陆地、水中验证四套制动常量。
4. 确认终点后约剩 `activeBlocks+8` 格时开始制动；倒数第 8 格入口按该格轨形达到对应原版沿轨目标。
5. 平轨交回保持缓存原版水平上限；坡轨交回沿轨目标为水平上限乘 `√2`，实体水平速度不得跳变。
6. 提前达到目标后保持；入口只允许浮点误差校正，随后最后 8 格由原版处理。
7. 降低 `maxSpeed` 后下一 tick 创建新制动阶段；目标高于自定义上限时不主动降低原版能力。
8. 制动中延长线路并重新满足有效激活长度时退出末端制动，按新阶段恢复加速。

## 异常、区块与回归

1. 高速撞方块、矿车或其他实体：降到缓存原版水平上限并退出接管，由原版决定后续反弹、推挤或停止。
2. 乘客和同一载具链不得被判为碰撞障碍；上下车不得让载人矿车回落到原版上限。
3. 接近未加载区块时停在最后已加载位置，冻结沿轨速度、方向和阶段；加载后无额外一 tick 加减速。
4. 超过 64 个子段时仅截断当 tick 位移并限频警告，保持模组状态。
5. 验证轨道拆除、动力轨断电、传送和多车交互不会产生 NaN、崩溃或错误接管。
6. 长水平直轨不得重新出现周期性顿挫；弯轨不得弹射起步。
7. 默认与实验控制器各完整复测一次；差异只应来自启动时缓存的原版交回上限。
8. 未安装模组的客户端可连接、乘坐、观察和下车，无未知 payload 或缺少模组错误。
